apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-sqllite.fullname" . }}
  labels:
    {{- include "test-sqllite.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-sqllite.selectorLabels" . | nindent 4 }}
